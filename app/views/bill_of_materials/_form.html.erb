<%= form_with(model: bill_of_material, local: true) do |form| %>
  <% if bill_of_material.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bill_of_material.errors.count, "error") %> prohibited this bill_of_material from being saved:</h2>

      <ul>
        <% bill_of_material.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :uuid %>
    <%= form.text_field :uuid %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
